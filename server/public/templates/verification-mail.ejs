<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Kanbask – Email Verification</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <style>
      /* =========================
         Key Styles for Kanbask Emails
         ========================= */
      body {
        margin: 0;
        padding: 0;
        background-color: #f5f7fa;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial,
          sans-serif;
        color: #1f2937;
        line-height: 1.6;
      }

      a {
        color: #3d83f6;
        text-decoration: none;
      }

      h1 {
        font-size: 24px;
        font-weight: 700;
        margin-bottom: 8px;
        text-align: center;
        color: #ffffff;
      }

      .wrapper {
        max-width: 600px;
        margin: 24px auto;
        border: 1px solid #e0e7ff;
        border-radius: 12px;
        overflow: hidden;
        background-color: #ffffff;
      }

      .header {
        background-color: #3d83f6;
        padding: 16px 24px;
      }

      .body {
        padding: 24px;
      }

      .footer {
        background-color: #f3f4f6;
        padding: 16px 24px;
        font-size: 14px;
        text-align: center;
        color: #6b7280;
      }

      .verification-code {
        margin: 24px auto;
        font-size: 40px;
        letter-spacing: 12px;
        text-align: center;
        color: #3d83f6;
        font-weight: 700;
      }

      p {
        margin-top: 12px;
        margin-bottom: 12px;
        font-size: 16px;
      }

      @media screen and (max-width: 480px) {
        .wrapper {
          margin: 16px;
        }
        .body {
          padding: 16px;
        }
        .header,
        .footer {
          padding: 12px;
        }
        h1 {
          font-size: 20px;
        }
        p {
          font-size: 15px;
        }
        .verification-code {
          font-size: 32px;
          letter-spacing: 8px;
        }
      }
    </style>
  </head>

  <body>
    <div class="wrapper">
      <!-- HEADER -->
      <div class="header">
        <h1>Email Verification</h1>
      </div>

      <!-- BODY CONTENT -->
      <div class="body">
        <p>Hi <strong><%= user.name %></strong>,</p>
        <p>
          Thanks for registering with <strong>Kanbask</strong>! To complete your registration,
          please use the verification code below:
        </p>

        <div class="verification-code"><%= verificationCode %></div>

        <p>
          This verification code will expire in <strong>10 minutes</strong>. If you did not create a
          Kanbask account, you can ignore this email.
        </p>
      </div>

      <!-- FOOTER -->
      <div class="footer">
        <p>Questions? Contact us at:</p>
        <p>
          <a href="mailto:kanbask.tech@gmail.com" style="color: #3d83f6">kanbask.tech@gmail.com</a>
        </p>
        <p style="margin-top: 8px; font-size: 13px; color: #9ca3af">
          © <%= new Date().getFullYear() %> Kanbask. All rights reserved.
        </p>
      </div>
    </div>
  </body>
</html>
